# 0.2.3 Release Verification

Date: 2026-09-13

## Release Candidate

- 54 test files passed: 366 tests passed, one Windows-inapplicable POSIX process-group test skipped.
- Web and CLI build passed.
- Installed-tarball checks passed: isolated installation, CLI shim, local self-check, real MCP stdio handshake, and bounded shutdown.
- Local self-check passed all five checks with version 0.2.3.
- Desktop 1280px and mobile 390px browser regression passed: bundled sample import, overview, cited error selection, detail visibility, back-button focus, and confirmed frozen export.
- Browser checks reported no console errors. Screenshots use bundled samples only and remain local.
- Production dependency audit: zero known vulnerabilities. Full audit: one low-severity development-only esbuild entry after compatible updates.
- Local verification output and browser snapshots are ignored by Git and excluded from the npm package.

## Remaining External Checks

- Desktop-side end-to-end verification is pending. The active task did not acquire TraceLens tools after registration. Protocol and browser checks are not a substitute for a new local desktop task actually calling the tools.
- npm authentication expired during release preparation, and the web-login attempt exited with an npm CLI error before authentication completed. The candidate remains on the feature branch; main, Pages, and npm have not been updated. Do not describe 0.2.3 as published until the registry confirms it.

## Desktop Acceptance

1. Enable and restart TraceLens under Settings > MCP servers on the intended local host.
2. Start a new local Codex task in the project under inspection.
3. Ask it to use TraceLens to find the latest abnormal run, cite event IDs, and open the corresponding evidence.
4. Verify actual `list_sessions`, `get_session_overview`, `get_event_detail`, and `get_viewer_link` calls, and that the viewer selects the same cited event.

Do not paste private log excerpts or authenticated viewer URLs into public release notes, issues, or comments.
